# Windy Concert

桌面端本地音乐播放器：以本地音乐库为核心，提供现代流媒体式交互体验的个人音乐平台。本文件是领域术语表，只定义概念，不记录实现决策（实现决策见 docs/adr/）。

## Language

### 音乐库核心

**音乐库（Library）**：
用户全部曲目的集合，由一个或多个音乐目录扫描聚合而成，存储于本地数据库。
_Avoid_: 文件夹集合、磁盘目录

**音乐目录（LibraryFolder）**：
用户显式授权给程序递归扫描的本地文件夹，可添加、删除、临时禁用。
_Avoid_: 监听目录、根目录

**曲目（Track）**：
一首可播放的音乐作品，拥有稳定 ID（与文件路径解耦）。一个 Track 对应一个音频文件，但文件移动不改变 Track 身份。
_Avoid_: 文件、歌曲文件、音频

**艺术家（Artist）**：
曲目的主表演者实体。MVP 中每个曲目关联单一主艺术家，原始艺术家字符串（含 feat. 部分）完整保留在曲目上。
_Avoid_: 歌手、表演者、艺人（不区分主/合作时）

**专辑（Album）**：
一组属于同一专辑发行（album + albumArtist 组合键）的曲目集合，拥有统一封面。

**收藏（Liked Songs）**：
用户标记喜欢的曲目集合。MVP 仅支持曲目级收藏（♡/♥）。
_Avoid_: 收藏夹、红心歌单

**歌单（Playlist）**：
用户手动创建并长期维护的有序曲目集合，与播放会话无关。
_Avoid_: 播放列表（易与 Queue 混淆）、列表

**播放队列（Queue）**：
当前播放会话中的曲目顺序，播放/切歌操作的对象。歌单是长期数据，队列是会话状态，二者严格区分。
_Avoid_: 播放列表、临时歌单

### 播放行为

**播放过一次（A Play）**：
一次曲目加载播放（loadTrack）即计一次：playCount 加一并写入一条播放历史。同一曲目内的暂停恢复、进度拖动不重复计数。
_Avoid_: 听完、完整播放

**播放历史（PlayHistory）**：
每次"播放过一次"产生的一条记录，用于最近播放、播放统计与远期推荐。

**曲目状态（Track Status）**：
文件在磁盘上的存在状态：available（正常）/ missing（文件消失）/ ignored（用户忽略）。missing 不删除数据库记录。

**不可播曲目（Unplayable Track）**：
已扫描入库、但因解码器限制（MVP 阶段 Chromium 原生格式集之外，如 APE/WMA/AIFF/DSF）暂不能播放的曲目。区别于 missing（文件不存在），0.5 引入 FFmpeg 后恢复可播。

**智能歌单（Smart Playlist）**：
按条件规则自动生成的歌单，本质是一条保存的查询（如"2020 年后添加的 FLAC"），不手工维护曲目。0.5 引入。
